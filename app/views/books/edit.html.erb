<h4>Editing Book</h4>

<% if @book.errors.any? %>
  <% if @book.errors.count == 1 %>
    <%= @book.errors.count %>error prohibited this book from being saved:
  <% else %>
    <%= @book.errors.count %>errors prohibited this book from being saved:
  <% end %>
  <ul>
    <% @book.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
  </ul>
<% end %>

<%= form_with model:@book do |f| %>
<div class="form">
  <h5>Title</h5>
  <%= f.text_field :title %>

  <h5>Body</h5>
  <%= f.text_area :body %>

  <div class="a"><%= f.submit ' Update Book ' %></div>
</div>
<% end %>

<%= link_to "Show", book_path(@book.id), class: "link-color" %>
|
<%= link_to "Back", books_path, class: "link-color" %>
